package javaMisc;

public class packages {
	
	//a package is a collection of classes
	//we need to 
	
	/*
	 * java.lang comes as default package you need not import it
	 * contains all the basic data variables like int, char, array, print etc
	 */
	
	/*
	 * java.util --> contains al the collection interfaces
	 */
	
	/*
	 * if we have two classes A and B in the same package
	 * then we can directly creating the object and we need not import it's packge
	 */

}
